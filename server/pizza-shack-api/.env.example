# Server Configuration
PORT=3000
NODE_ENV=development
API_VERSION=v1

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# JWT Configuration
# For development: Set JWT_SKIP_VERIFICATION=true to skip signature validation
# For production: Set JWT_SKIP_VERIFICATION=false and configure JWKS endpoint
JWT_SKIP_VERIFICATION=true
JWT_JWKS_URI=https://localhost:9443/oauth2/jwks
JWT_ISSUER=https://localhost:9443/oauth2/token

# JWT_AUDIENCE is optional - validates the 'aud' claim if set
# Can be a single client_id or comma-separated list for multiple apps
# Example: your-client-id-1,your-client-id-2
# Leave empty or comment out to skip audience validation
# JWT_AUDIENCE=your-client-id
